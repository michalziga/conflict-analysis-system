# ── Core scraping ─────────────────────────────────────────────────────────
feedparser>=6.0
newspaper3k>=0.2.8
lxml>=5.0
lxml_html_clean>=0.1       # required by newspaper3k with lxml >= 5.2
requests>=2.31
beautifulsoup4>=4.12

# ── NLP ───────────────────────────────────────────────────────────────────
nltk>=3.8                  # punkt tokeniser for newspaper3k .nlp()
langdetect>=1.0.9          # language detection

# ── Analysis / data ───────────────────────────────────────────────────────
# pandas and numpy are optional — only needed for DataFrame operations
# pandas>=2.0
# numpy>=1.26

# ── Downstream NLP pipeline (optional) ───────────────────────────────────
# Uncomment to enable NER, embeddings, and sentiment enrichment:
# spacy>=3.7
# sentence-transformers>=2.7
# transformers>=4.40
# torch>=2.0

# ── Dev / test ────────────────────────────────────────────────────────────
# pytest>=8.0
# pytest-cov>=5.0
